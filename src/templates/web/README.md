# Shibumi web app

Bun, Hono, Zod, and Alpine. Everything here is your source.

```sh
bun install
bun dev
```

Deploy to your VPS with `bun ship:setup`, then `bun ship`. See `agents.md` for the project map and the rules tests enforce.
